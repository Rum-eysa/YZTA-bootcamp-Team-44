import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "İlanlarım - CareerTrack",
};

export default function ListingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
