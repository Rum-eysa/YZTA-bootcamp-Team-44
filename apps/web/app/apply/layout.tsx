import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "İş İlanı Ekleme - CareerTrack",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
